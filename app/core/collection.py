"""수집 패키지가 공유하는 정책 상수.

시장 시간대(`_time.py`)와 달리 이건 프로젝트가 정한 운영 규칙이라 따로 둔다.
"""

from datetime import date


# 확정값 백필의 기준 시작일. 모든 확정값 지표가 같은 기준을 쓴다.
#
# 소스가 이 날짜부터 주지 못하면(상장이 늦거나 보관 기간이 짧으면) 실패로 보지 않고
# 소스가 주는 가장 이른 데이터부터 담는다. 빈 구간을 메우려고 다른 소스를 끌어오지 않는다 —
# 소스가 섞이면 조정 기준이 달라져 같은 계열 안에서 가격이 튄다.
#
# 실제로 어디부터 담겼는지는 각 수집 로그의 first_ts·first_date로 확인한다.
BACKFILL_START = date(2025, 1, 1)

# 백필을 부를 때 날짜를 매번 적지 않도록 각 패키지 `__main__`이 받는 명령행 인자.
BACKFILL_KEYWORD = "backfill"

# KIS REST 호출 사이의 최소 간격(초).
#
# 백필은 종목·거래일마다 여러 번 부르는데 쉬지 않고 보내면 초당 거래건수 제한
# (EGW00201)에 걸린다. 제한 응답은 HTTP 500으로 와서 예외가 되고 그 시점에 백필이 죽는다.
# 정기 수집은 호출 수가 적어 이 대기가 문제되지 않는다.
KIS_REQUEST_INTERVAL_SECONDS = 0.5
