"""signal 도메인 DB 엔티티 (§8.1).

여기 구현할 것:
- `Signal`         — signals 테이블 (instrument_id, news_id, direction, magnitude, confidence, rationale, model)
- `SignalOutcome`  — signal_outcomes 테이블 (signal_id, horizon, return_pct, hit)
"""
